#############################################################################
#
# OpenSLP configuration file
#
# Format and contents conform to specification in IETF RFC 2614 so the
# comments use the language of the RFC.  In OpenSLP, SLPD operates as an SA
# and a DA.  The SLP UA functionality is encapsulated by SLPLIB.
#
#############################################################################


#### MANAGED BY PUPPET#####

<% if @is_da  %>
net.slp.isDA = true
net.slp.DASyncReg = <%= @da_sync %>
net.slp.isDABackup = <%= @da_backup %>
net.slp.DABackupInterval = <%= @da_backup_interval %>
<% else %>
net.slp.isDA = false
<% end %>
net.slp.useScopes = <%= @scope.flatten.join(", ") %>
net.slp.DAAddresses = <%= @da_addresses.flatten.join(", ") %>
net.slp.isBroadcastOnly = <%= @broadcast_only %>

